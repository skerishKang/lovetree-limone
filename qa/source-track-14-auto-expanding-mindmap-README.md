# Track14 visual evidence

The dedicated Issue #488 browser workflow renders the byte-pinned Track14 V2.4 HTML directly from the repository snapshot and the React/Next canonical PATH donor at identical viewports.

Evidence is uploaded as a GitHub Actions artifact and includes source, native, side-by-side and overlay captures. The evidence must be visually inspected before the worker may report `VISUAL_FIDELITY=PASS`.

No screenshot is treated as product truth or automatically accepted merely because the browser test exits zero.
