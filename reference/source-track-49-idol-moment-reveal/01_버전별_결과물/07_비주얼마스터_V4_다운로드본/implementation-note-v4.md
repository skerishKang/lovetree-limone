GENERATED VISUAL MASTER EXACT-BUILD CANDIDATE

This version uses the newly approved generated visual master as the single visual source of truth.

Implementation strategy:
1. Slice the approved visual master into 5 locked desktop sections.
2. Overlay interactive hotspots, CTA buttons, filter controls, and save-form behavior without changing visual layout.
3. Build a mobile-specific stacked layout using the exact-build asset crops derived from the master.
4. Keep motion minimal: fade-in, subtle hotspot lift, gentle twinkle.

Notes:
- No hover reveal that swaps faces or text.
- No new cast roster.
- No mountain / cyber / dashboard additions.
- Future media links are deliberately empty string slots in JSON/config and can be replaced later.
