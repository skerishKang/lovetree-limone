name: V4 P2 memory contracts

on:
  push:
    branches:
      - feat/v4-p2-memory-contracts-20260809
  pull_request:
    branches:
      - feat/v4-final-release-candidate-20260809

permissions:
  contents: write

jobs:
  generate-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout P2 branch
        uses: actions/checkout@v4
        with:
          ref: feat/v4-p2-memory-contracts-20260809
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply guarded server patches
        run: |
          node scripts/apply-v4-p2-memory-contracts.mjs
          node scripts/apply-v4-p2-nullable-parent.mjs

      - name: Install exact dependencies
        run: npm ci

      - name: Generate additive Drizzle migration
        run: npm run db:generate

      - name: Commit generated server and migration changes
        shell: bash
        run: |
          if git diff --quiet -- server/api/trees.ts server/api/memories.ts drizzle; then
            echo "P2 server patches and migration already synchronized"
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add server/api/trees.ts server/api/memories.ts drizzle
            git commit -m "feat(v4): synchronize P2 Memory contracts"
            git push origin HEAD:feat/v4-p2-memory-contracts-20260809
          fi

      - name: P2 contract tests
        run: node --import tsx --test tests/v4-p2-memory-contracts.test.mjs

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Database schema check
        run: npm run db:check
