name: Track35 LP archive native browser QA

on:
  pull_request:
    branches:
      - main
    paths:
      - 'package.json'
      - 'package-lock.json'
      - 'next.config.ts'
      - 'vite.config.ts'
      - 'postcss.config.mjs'
      - 'tsconfig.json'
      - 'app/layout.tsx'
      - 'app/globals.css'
      - 'app/v4/trees/**/archive/lp/**'
      - 'lib/source-track-35/**'
      - 'lib/moment-model.ts'
      - 'lib/use-tree-moments.ts'
      - 'reference/source-tracks-snapshot/35_LP플레이어/**'
      - 'tests/track35-lp-player-contract.test.mjs'
      - 'qa/track35-lp-player-browser-qa.mjs'
      - '.github/workflows/track35-lp-player-browser-qa.yml'
      - 'drizzle.config.ts'
      - 'db/**'
      - 'drizzle/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  track35-lp-archive-native:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Track35 focused contracts
        run: node --import tsx --test tests/track35-lp-player-contract.test.mjs
      - name: Lint
        run: npm run lint
      - name: Typecheck
        run: npm run typecheck
      - name: Database schema check
        run: npm run db:check
      - name: Build
        run: npm run build
      - name: Prepare Playwright runtime
        run: npx playwright install --with-deps chromium
      - name: Run Track35 LP archive browser QA
        shell: bash
        run: |
          set -euo pipefail
          npm start > /tmp/track35-browser-qa-server.log 2>&1 &
          server_pid=$!
          cleanup() {
            kill "$server_pid" 2>/dev/null || true
            wait "$server_pid" 2>/dev/null || true
          }
          trap cleanup EXIT
          ready=0
          for attempt in {1..90}; do
            if curl --fail --silent --show-error "http://127.0.0.1:3000/v4" > /dev/null; then
              ready=1
              break
            fi
            if ! kill -0 "$server_pid" 2>/dev/null; then
              cat /tmp/track35-browser-qa-server.log
              exit 1
            fi
            sleep 1
          done
          if [ "$ready" -ne 1 ]; then
            cat /tmp/track35-browser-qa-server.log
            exit 1
          fi
          mkdir -p /tmp/track35-browser-qa
          V4_BASE_URL="http://127.0.0.1:3000" \
            TRACK35_SCREENSHOT_DIR="/tmp/track35-browser-qa" \
            node qa/track35-lp-player-browser-qa.mjs
      - name: Upload Track35 browser QA evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: track35-lp-archive-native-browser-evidence
          path: /tmp/track35-browser-qa/*.png
          if-no-files-found: warn
