name: Five-source isolated preview deploy (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded preview deploy path
        run: |
          echo 'This bootstrap workflow is retired.'
          echo 'Use .github/workflows/five-source-visual-repair-preview.yml for guarded preview deploy, smoke, and live five-view QA.'
          echo 'No deployment is performed by this workflow.'
