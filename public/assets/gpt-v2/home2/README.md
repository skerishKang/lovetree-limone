# home2 sliced asset scaffold

Expected files for the image-sliced experiment:

- `header.png`
- `hero-left.png`
- `hero-right.png`
- `features.png`
- `curation.png`
- `bg-soft.png` (optional)

This folder was created as the target location for actual sliced image assets.

Current limitation:
- The repository scaffold can be created from here.
- Actual binary image slices still need to be generated and committed as image files.

After the images are added, `pages/gpt-v2/home2.html` can reference them directly.
