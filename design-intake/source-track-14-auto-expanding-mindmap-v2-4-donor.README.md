# Track14 Auto-expanding Mindmap V2.4 — PATH donor

Issue: #488  
Parent decision ledger: #344

## Source authority

- Drive family: `14_자동전개마인드맵_템플릿컴포저`
- current executable: `01_자동전개마인드맵_현재채택_템플릿컴포저_v2-4.html`
- Drive file ID: `1HSYYqBTf6R4yyTBVHmnkZ_cb20aTWFEK`
- bytes: `87,256`
- SHA256: `c30d6e1ce861bffbeccbeaeab515a8d51fd4bf00704f7093f3b1aab505cc3d4c`
- repository blob: `26488eb6f4a56b3611b66456c156b487674a64d3`
- repository snapshot is byte-identical to the current Drive object.

## Product disposition

`USE_AS_VISUAL_FUNCTION_DONOR`

The current canonical PATH/graph already owns real Moment identity, `parentId`, `connectionReason`, selection and WHY NEXT presentation. Track14 does not justify a second graph backend or a second writable topology.

The bounded native lens donates:

- staged auto-expanding hierarchy;
- curved branch/Connection treatment;
- branch-focus and unrelated-context dimming;
- paper/cream/glass spatial composition;
- pan/zoom;
- a presentation-only layout composer.

## Native route

`/trees/:treeId/graph/mindmap`

Runtime authority remains `useTreeMoments(treeId)` and canonical `TreeMomentView` data. Layout, unfold progress, pan/zoom and branch focus are presentation state only.

The source composer contains editable sample templates, local media and writable node/edge demo state. The native donor deliberately does not copy those as product truth. Its composer changes only visual layout; it cannot write `parentId` or `connectionReason`.

## Fidelity contract

Dedicated browser QA runs the exact source snapshot and the native route at matched viewports. It captures source/native/side-by-side evidence for:

- initial state;
- fully expanded state;
- selected-branch state;
- desktop `1280×800`;
- mobile `390×844`;
- native compact `320×720`;
- reduced motion.

The same QA checks touch pan, keyboard selection, visible focus reachability, layout-composer topology preservation, horizontal overflow and console/page errors.

Automated screenshots are evidence for direct visual review; they are not, by themselves, a source-fidelity approval.

## Parallel safety

This Track14 lane does not edit:

- `scripts/design-fidelity-validation-inventory.mjs`
- `design-intake/master-design-coverage.json`
- `tests/master-design-coverage-contract.test.mjs`
- shared navigation/registry
- Auth/API/DB/schema
- PR #191

Because the route carries a dedicated browser workflow, the fail-closed global Design Fidelity inventory is expected to require CENTRAL registration after Track14-specific QA is complete.
