# CLEAN-108 DUAL_VARIANT replay plugin boundary

This directory contains the additive SRC068 explicit-plugin replay proof used by issue #611. DUAL_VARIANT replay keeps `{sourceId, variant}` identity, requires explicit A/B selection, leaves the generic single-executable replay path fail-closed, and writes candidate evidence only outside Source authority capsules.
