# Firebase client configuration (public Web API keys)
# Vinext builds use production mode; for a staging build, place these values
# in the ignored .env.production.local file (never commit that file).
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
